# 本地模式改造验收

验收日期：2026-09-09。环境：Windows、Python 3.13、Node.js 22。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| 后端 unittest | 445 项通过 |
| 前端 Vitest | 48 个文件、128 项通过 |
| 前端类型检查与生产构建 | 通过 |
| `scripts/check-startup.py` 空数据启动 | 通过 |
| `git diff --check` | 通过 |

工作区测试覆盖空数据库、单份历史资料、多份历史资料选择及数据库重开后的选择持久化；沿用历史 user_id、会话及默认助手。补充了手动默认模型进入模型列表、音乐子进程状态和删除请求失败等回归检查。

## 从源码安装

将当前修改后的源码复制到独立目录 `AgentBI-clean-check`，从空虚拟环境与依赖目录运行启动脚本，完成后端、前端和内置音乐服务依赖安装。该方式验证当前待提交源码；提交后仍可用远程仓库重新克隆复验。

使用独立端口启动后，后端与前端实例探针均通过，音乐状态返回 `enabled: true`、`ready: true`。重复运行启动脚本可复用同一目录的服务。Windows 实际对话暴露的时区数据缺失已通过加入 `tzdata` 修复，并加入启动检查。

## 浏览器验收

使用隔离的 Chrome 会话完成：

- 直接进入本地工作台，刷新后保持本地资料。
- 配置 OpenAI 兼容模型端点，手动默认模型可选。
- 新建会话、发送消息、接收流式回复，刷新后恢复历史。
- 旧 `/login`、`/about` 地址返回工作台，404 页面可返回首页。
- 多份历史资料选择界面可选择并进入；该界面使用接口模拟数据，真实 SQLite 持久化由后端测试覆盖。
- 桌面 1440 × 960 与移动端 390 × 844 页面检查，移动端首页横向溢出检查通过。

对话使用本地模拟模型服务完成，验证请求、流式响应及持久化链路。真实第三方模型、语音、图片、视频和邮件能力仍需结合各自服务配置验收。GitHub Actions 工作流已添加，远程执行结果待提交后产生。

页面截图见 [工作台](screenshots/workspace.png) 与 [Studio](screenshots/studio.png)。
