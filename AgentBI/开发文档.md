# AgentBI 后端

FastAPI + SQLite，本地资料、聊天 DAG、模型配置、助手、记忆、Playground 和测试数据保存在本地数据库，上传与生成资产存入本地附件目录。

`GET /workspace` 初始化工作区，`PUT /workspace` 选择既有资料。空库自动创建本地用户。SMTP 为独立邮件能力。

运行入口见 [根 README](../README.md)，架构和验证方式见 [开发说明](../docs/development.md)。
