# AgentBI 后端说明

本文件原先记录的是上一代后端实现，已不再适用于当前项目。

当前后端以本地 SQLite 为唯一持久化实现：用户资料、验证码、聊天 DAG、提供商配置、助手和邮件收件人均由 `SqliteChatRepository` 管理；登录使用确定性的 `LoginService`，邮件能力由 `EmailAgent` 调用受限的本地收件人检索和 SMTP 工具。

请以 [项目开发总览](../docs/AgentBI_开发总览.md) 为准。
